# shadowtraffic

ShadowTraffic generator config (`orders-raw.json`) that produces Avro `orders.raw` events through Schema Registry with deliberate dirty rows, plus the exact `docker run` command and a `.env.example`. Populated in Phase 3 by [prompts/03-producer.md](../prompts/03-producer.md). Spec and dirty-row rates: [docs/03-producer-spec.md](../docs/03-producer-spec.md).

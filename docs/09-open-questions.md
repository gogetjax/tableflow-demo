# 09 — Open questions

Claims below are plausible from docs but unverified. Each gets a smoke test in its phase; record the result here and only then let the README state it as fact.

| ID | Question | Phase | How to verify | Result |
|---|---|---|---|---|
| Q1 | How is dual format (ICEBERG + DELTA) expressed in the CLI, REST API, and Terraform provider? CLI doc says "one of". | 5 | Try Terraform `table_formats = ["ICEBERG","DELTA"]`; fall back to REST `PATCH`/`POST` with array; then CLI. Record which worked and the provider version. | |
| Q2 | Exact argument names for `confluent_tableflow_topic`, `confluent_catalog_integration`, `confluent_provider_integration` at the pinned provider version. | 2, 5 | Registry docs page for the pinned version; `terraform validate`. | **Provider 2.86.0.** `confluent_provider_integration`: `display_name`, `environment{id}`, `aws{customer_role_arn}`; exports `aws[0].iam_role_arn`, `aws[0].external_id` (applied Phase 2). `confluent_tableflow_topic`: `byob_aws{bucket_name, provider_integration_id}` (not `byos_aws`), `table_formats` list of `ICEBERG`/`DELTA`, `retention_ms`, `error_handling{mode, log_target}`, `credentials{key,secret}` = Tableflow API key. `confluent_catalog_integration`: `aws_glue{provider_integration_id}`, `credentials`. Source: provider repo `docs/resources` at tag v2.86.0. Apply-time verification of the last two in Phase 5. |
| Q3 | What Glue table name does Tableflow produce for topic `orders.clean` (dot in name)? | 5 | `aws glue get-tables --database-name <cluster-id>` | |
| Q4 | S3 layout for a dual-format topic: are Parquet data files shared between Iceberg and Delta or duplicated? Where is the Delta root path? | 5 | `aws s3 ls --recursive` after two commits; compare data file counts referenced by Iceberg manifest vs `_delta_log`. | |
| Q5 | Does a Flink-created sink table with `PRIMARY KEY ... NOT ENFORCED` and `changelog.mode='append'` produce a key schema Tableflow accepts? | 4–5 | Enable Tableflow on `orders.clean`; check status. | |
| Q6 | Avro `decimal` logical type mapping through Tableflow to both formats. | 3 | Only if a decimal column is added; otherwise keep `long` cents. | skipped by design |
| Q7 | Does the `LOG` error-handling strategy exist in the pinned Terraform provider? | 5 | Registry docs; else use `SKIP` and note it. | Docs for 2.86.0: `error_handling.mode` accepts `SUSPEND`, `SKIP`, `LOG` (default `SUSPEND`); `log_target` names the DLQ topic. Changelog 2.51.0 added the block, 2.56.0 fixed create with `skip`/`log`. Apply-time confirmation in Phase 5. |
| Q8 | Minimum IAM verbs for `tableflow-writer` per the current BYOS quick start. | 1–2 | Copy from the Confluent-generated policy in the Console during PI setup; diff against 02. | **S3 (Phase 1):** from [Configure Storage](https://docs.confluent.io/cloud/current/topics/tableflow/how-to-guides/configure-storage.html): bucket `s3:GetBucketLocation`, `s3:ListBucketMultipartUploads`, `s3:ListBucket`; objects `s3:PutObject`, `s3:PutObjectTagging`, `s3:GetObject`, `s3:DeleteObject`, `s3:AbortMultipartUpload`, `s3:ListMultipartUploadParts`. Three more than 02 listed (`PutObjectTagging`, `ListBucketMultipartUploads`, `ListMultipartUploadParts`); implemented in `terraform/aws/iam-writer.tf`. **Glue:** not published in docs ([Glue integration page](https://docs.confluent.io/cloud/current/topics/tableflow/how-to-guides/catalog-integration/integrate-with-aws-glue-catalog.html) shows only the Lake Formation variant); the Console generates the template. Using the 02 superset; diff against the Console template in Phase 5. |
| Q9 | Can `consumer-iceberg` read via Glue's Iceberg REST endpoint from DuckDB with SigV4, from the isolated subnet? Optional. | 6 | Try; if it needs Lake Formation, document and skip. | |
| Q10 | Databricks path-based read of a Tableflow Delta table (no Unity integration). The catalog doc says Delta tables are "available only through Unity Catalog integration"; the overview and the private-network workaround imply path reads work. | 6 | `CREATE TABLE ... USING DELTA LOCATION` in any workspace, or delta-rs as proxy. | |
